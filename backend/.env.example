# Server Configuration
# Specifies the running environment (development/production) and server host details.
NODE_ENV=development
PORT=3000
API_URL=http://localhost:3000

# Database
# Connection URL for the PostgreSQL database (using service name for Docker).
DATABASE_URL="postgresql://<DB_USER>:<DB_PASSWORD>@postgres:5432/crescite?schema=public"
# Local credentials for developers using the default docker-compose setup
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=crescite

# JWT Authentication
# Secret key used to sign and verify JSON Web Tokens (must be long and random).
JWT_SECRET=
JWT_EXPIRES_IN=7d

# AWS S3
# Credentials and region for connecting to AWS S3 for file storage.
AWS_REGION=us-east-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=

# Redis
# Connection details for the Redis cache (using service name for Docker).
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# RabbitMQ
# Connection URL for the RabbitMQ message broker.
RABBITMQ_URL=amqp://<MQ_USER>:<MQ_PASSWORD>@rabbitmq:5672
RABBITMQ_QUEUE=cas_processing_queue
# Local credentials for developers using the default docker-compose setup
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest

# CORS
# Frontend URL allowed to access the API.
CORS_ORIGIN=http://localhost:5173

# Worker Configuration
# Number of concurrent tasks the worker service should process.
WORKER_CONCURRENCY=4